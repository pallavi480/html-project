// given an array print the position (starting with 1) and the element in a single line

let name = ["khushi","prerna","priti","poonam"];

for(let i=0; i<name.length;i++){
console.log(i+1,name[i])

}


let arr =[1,2,3,4,5,66,77,89,90,67];
let sum = 0;

for(let i=0;i<=arr.length;i++){

    // console.log(arr[i])

    if(arr[i]%2==0){
        sum = sum+sum+arr[i]
        console.log(sum)
    }
}

    //     console.log("even no is",arr[i])
    // }else{
    //     console.log("odd no is",arr[i])
    // 
